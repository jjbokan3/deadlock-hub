# Core (always needed)
requests>=2.28
rich>=13.0

# LLM providers (install what you need)
# pip install anthropic      # for --llm claude
# pip install openai         # for --llm openai
# Ollama uses requests only  # for --llm ollama
